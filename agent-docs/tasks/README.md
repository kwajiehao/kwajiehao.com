# Tasks

Store generated task specs here using:

```text
YYYY-MM-DD-short-slug.md
```

Task files are working artifacts. Keep them concise, update acceptance criteria as evidence arrives, and close them when the main agent has verified the result.
